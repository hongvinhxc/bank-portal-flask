Details about a account
---
tags:
  - products
parameters:
  - name: accountid
    in: path
    type: string
    required: true
responses:
  200:
    description: Account model
    schema:
      $ref: '#/definitions/Account'
  404:
    description: Account not found